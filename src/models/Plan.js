// const mongoose = require('mongoose');
// const PlanSchema = new mongoose.Schema({
//     name: { type: String, required: true }, // e.g., One Month, Three Months
//     price: { type: Number, required: true }, // Fixed cost
//     durationInDays: { type: Number, required: true }, // Subscription period in days
//     discountPercentage: { type: Number, default: 0 }, // Discount on the base price
//     createdAt: { type: Date, default: Date.now },
//     updatedAt: { type: Date, default: Date.now },
// });

// module.exports = mongoose.model('Plan', PlanSchema);


